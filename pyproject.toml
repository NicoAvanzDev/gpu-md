[project]
name = "gpu-md-training"
version = "0.1.0"
description = "Train a compact Markdown classifier with PyTorch and CUDA; export to WebGPU"
requires-python = ">=3.11,<3.14"
dependencies = ["torch>=2.8,<3", "numpy>=2.3,<3"]

[tool.uv.sources]
torch = { index = "pytorch-cu128" }

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true
