export const samples: Record<string, string> = {
  'Field notes':
    "# A small model. A readable web.\n\nWhat if rendering Markdown started with a tiny neural network?\n\n**gpu-md** sketches each line, predicts its role, and assembles the result into HTML. The model runs locally, right in your browser.\n\n## From source to structure\n\n1. Turn characters into a compact feature vector.\n2. Run two small neural layers on WebGPU.\n3. Turn the predictions into a document.\n\n> A different way to get from plain text to something worth reading.\n\n### The experiment\n\n- [x] Train a tiny classifier with PyTorch + CUDA\n- [x] Export its weights as signed 8-bit integers\n- [x] Run the model in a WebGPU compute shader\n- [x] Pass all 652 CommonMark specification examples\n- [ ] Explore inputs beyond the specification suite\n\n```ts\nimport { createRenderer } from './gpu-md.js'\n\nconst md = createRenderer()\nconst { html, timings } = await md.render('# Hello, GPU')\n```\n\n| Stage | Where it runs |\n| :--- | :--- |\n| Training | PyTorch · CUDA |\n| Line predictions | WebGPU compute |\n| HTML assembly | JavaScript worker |\n\nThis is an **experimental Markdown renderer**. Predictions can be wrong. Edit the source and inspect the line labels to see what the model thinks.\n\n---\n\nInspired by [gpu-lexer](https://gpu-lexer.vercel.app/). Small models, interesting possibilities.\n",
  'Kitchen sink':
    '# Heading one\n## Heading two\n### Heading three\n\nA paragraph with **strong**, *emphasis*, ~~strikethrough~~, and `inline code`.\n\nSetext heading\n==============\n\n> A blockquote with **formatted text**.\n>\n> Another paragraph inside the quote.\n\n- First item\n- Second item\n  - A nested item\n  - Another nested item\n- Third item\n\n3. An ordered list starting at three\n4. The next item\n\n- [x] Completed task\n- [ ] An open task\n\n| Feature | Support |\n| :--- | ---: |\n| Headings | Yes |\n| Lists | Yes |\n| Tables | Yes |\n\n````md\n# This is code, not a heading\n```\nFences can contain shorter fences.\n````\n\n    An indented code block.\n\nA [link with a title](https://example.com "Example").\n\nUnicode works too: café, 世界, 🚀.\n\nEscaped \\*asterisks\\* and snake_case_names.\n\n---\n\nRaw HTML is displayed as text: <script>alert(\'hello\')</script>\n',
  'Edge cases':
    "# A model can be wrong\n\nThese examples exercise tricky syntax and intentional safety boundaries.\n\n## Literal syntax\n\n\\# This is not a heading.\n\n####### Neither is this.\n\n`**This is code, not strong text.**`\n\n## Nesting\n\n- A list item\n  - A child item\n    - A grandchild\n\n> An outer quote.\n> > An inner quote.\n\n## Forward reference links\n\n[A reference link][reference]\n\n[reference]: https://example.com\n\n## Unsafe destinations stay literal\n\n[Click me](javascript:alert(1))\n\n<img src=x onerror=alert(1)>\n\n## An unfinished fence\n\n```js\n// The remaining document stays inside the code block.\nconst message = '# Still code';\n",
}
